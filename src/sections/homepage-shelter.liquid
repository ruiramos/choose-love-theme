{%- assign collection = collections[section.settings.collection] -%}

<div class="homepage-product-grid-stage stage-camp">
  <div class="one-whole row-2">
    <!-- tent -->
    {%- assign product = collection.products[0] -%}
    {%- assign current_variant = product.selected_or_first_available_variant -%}
    <a href="{{ product.url }}" class="homepage-product" data-section-type="product" itemscope itemtype="http://schema.org/Product">
      <meta itemprop="name" content="{{ product.title }}">
      <meta itemprop="url" content="{{ shop.url }}{{ product.url }}">
      <meta itemprop="image" content="{{ product.featured_image | img_url: '800x' }}">
      <meta itemprop="description" content="{{ product.description | strip_html | escape }}">

      <img src="{{ product.featured_image | img_url: '800x' }}" />

      <span class="product-info">
        <span class="product-name">{{ product.title }}</span>
        <span class="product-price">{{ current_variant.price | money }}</span>
      </span>
    </a>
  </div>
  <div class="one-whole row-2">
    <div class="one-third row-12">
    <!-- nappies -->
      {%- assign product = collection.products[1] -%}
      {%- assign current_variant = product.selected_or_first_available_variant -%}
      <a href="{{ product.url }}" class="homepage-product" data-section-type="product" itemscope itemtype="http://schema.org/Product">
        <meta itemprop="name" content="{{ product.title }}">
        <meta itemprop="url" content="{{ shop.url }}{{ product.url }}">
        <meta itemprop="image" content="{{ product.featured_image | img_url: '800x' }}">
        <meta itemprop="description" content="{{ product.description | strip_html | escape }}">

        <img src="{{ product.featured_image | img_url: '800x' }}" />

      <span class="product-info">
        <span class="product-name">{{ product.title }}</span>
        <span class="product-price">{{ current_variant.price | money }}</span>
      </span>
      </a>
    </div>
    <div class="two-thirds row-12">
    <!-- sleeping bag -->
      {%- assign product = collection.products[2] -%}
      {%- assign current_variant = product.selected_or_first_available_variant -%}
      <a href="{{ product.url }}" class="homepage-product" data-section-type="product" itemscope itemtype="http://schema.org/Product">
        <meta itemprop="name" content="{{ product.title }}">
        <meta itemprop="url" content="{{ shop.url }}{{ product.url }}">
        <meta itemprop="image" content="{{ product.featured_image | img_url: '800x' }}">
        <meta itemprop="description" content="{{ product.description | strip_html | escape }}">

        <img src="{{ product.featured_image | img_url: '800x' }}" />

      <span class="product-info">
        <span class="product-name">{{ product.title }}</span>
        <span class="product-price">{{ current_variant.price | money }}</span>
      </span>
      </a>
    </div>
  </div>
  <div class="one-whole row-5">
    <div class="one-whole row-6" data-group="hygiene">
    <!-- hygiene 1 -->
      {%- assign product = collection.products[3] -%}
      {%- assign current_variant = product.selected_or_first_available_variant -%}
      <a href="{{ product.url }}" class="homepage-product" data-section-type="product" itemscope itemtype="http://schema.org/Product">
        <meta itemprop="name" content="{{ product.title }}">
        <meta itemprop="url" content="{{ shop.url }}{{ product.url }}">
        <meta itemprop="image" content="{{ product.featured_image | img_url: '800x' }}">
        <meta itemprop="description" content="{{ product.description | strip_html | escape }}">

        <img src="{{ product.featured_image | img_url: '800x' }}" />

        <span class="product-info">
          <span class="product-name">{{ product.title }}</span>
          <span class="product-price">{{ current_variant.price | money }}</span>
        </span>
      </a>
    </div>
    <div class="one-whole row-6">
      <div class="one-half row-12" data-group="hygiene">
    <!-- hygiene 2 -->
        {%- assign product = collection.products[3] -%}
        {%- assign current_variant = product.selected_or_first_available_variant -%}
        <a href="{{ product.url }}" class="homepage-product" data-section-type="product" itemscope itemtype="http://schema.org/Product">
          <meta itemprop="name" content="{{ product.title }}">
          <meta itemprop="url" content="{{ shop.url }}{{ product.url }}">
          <meta itemprop="image" content="{{ product.featured_image | img_url: '800x' }}">
          <meta itemprop="description" content="{{ product.description | strip_html | escape }}">

          <img src="{{ product.images[2] | img_url: '800x' }}" />

      <span class="product-info">
        <span class="product-name">{{ product.title }}</span>
        <span class="product-price">{{ current_variant.price | money }}</span>
      </span>
        </a>
      </div>
      <div class="one-half row-12">
    <!-- support -->
        {%- assign product = collection.products[6] -%}
        {%- assign current_variant = product.selected_or_first_available_variant -%}
        <a href="{{ product.url }}" class="homepage-product" data-section-type="product" itemscope itemtype="http://schema.org/Product">
          <meta itemprop="name" content="{{ product.title }}">
          <meta itemprop="url" content="{{ shop.url }}{{ product.url }}">
          <meta itemprop="image" content="{{ product.featured_image | img_url: '800x' }}">
          <meta itemprop="description" content="{{ product.description | strip_html | escape }}">

          <img src="{{ product.featured_image | img_url: '800x' }}" />

      <span class="product-info">
        <span class="product-name">{{ product.title }}</span>
        <span class="product-price">{{ current_variant.price | money }}</span>
      </span>
        </a>
      </div>
    </div>
  </div>
  <div class="one-whole row-3">
    <div class="seven-twelfths row-12">
    <!-- food -->
      {%- assign product = collection.products[4] -%}
      {%- assign current_variant = product.selected_or_first_available_variant -%}
      <a href="{{ product.url }}" class="homepage-product" data-section-type="product" itemscope itemtype="http://schema.org/Product">
        <meta itemprop="name" content="{{ product.title }}">
        <meta itemprop="url" content="{{ shop.url }}{{ product.url }}">
        <meta itemprop="image" content="{{ product.featured_image | img_url: '800x' }}">
        <meta itemprop="description" content="{{ product.description | strip_html | escape }}">

        <img src="{{ product.featured_image | img_url: '800x' }}" />

      <span class="product-info">
        <span class="product-name">{{ product.title }}</span>
        <span class="product-price">{{ current_variant.price | money }}</span>
      </span>
        </a>
      </a>
    </div>
    <div class="five-twelfths row-12">
    <!-- boots -->
      {%- assign product = collection.products[5] -%}
      {%- assign current_variant = product.selected_or_first_available_variant -%}
      <a href="{{ product.url }}" class="homepage-product" data-section-type="product" itemscope itemtype="http://schema.org/Product">
        <meta itemprop="name" content="{{ product.title }}">
        <meta itemprop="url" content="{{ shop.url }}{{ product.url }}">
        <meta itemprop="image" content="{{ product.featured_image | img_url: '800x' }}">
        <meta itemprop="description" content="{{ product.description | strip_html | escape }}">

        <img src="{{ product.featured_image | img_url: '800x' }}" />

      <span class="product-info">
        <span class="product-name">{{ product.title }}</span>
        <span class="product-price">{{ current_variant.price | money }}</span>
      </span>
      </a>
    </div>
  </div>
</div>

{% comment %}
<div class="homepage-product-grid" data-section-id={{ section.id }}>
{% for product in collection.products limit: 6 %}
  <a href="{{ product.url  }}" class="homepage-product">
    {% if product.featured_image != blank %}
      {{ product.featured_image.src | img_url: '200x200' | img_tag: product.title }}
    {% endif %}
    <div class="homepage-product-info">
      <p>{{ product.title }}</p>
      <p>{{ product.price | money }}</p>
    </div>
  </a>
{% endfor %}
</div>
{% endcomment %}

{% schema %}
  {
    "name": "Homepage Shelter",
    "settings": [
      {
        "id": "collection",
        "type": "collection",
        "label": "Collection"
      }
    ],
    "presets": [
      {
        "name": "Homepage Shelter",
        "category": "Collection"
      }
    ]
  }

{% endschema %}
